spring:
  config:
    import: mail-templates.yml
  mail:
    host: host
    port: 587
    username: username
    password: username
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
activity:
  root:
    path: /DATA/